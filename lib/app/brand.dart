import 'package:flutter/material.dart';

/// Единственный файл, отличающийся между приложениями конвейера.
/// Сгенерирован _pipeline/generate_v2.ps1 — править вручную нет смысла.

const String kAppTitle = 'Rainbe';
const String kProductTitle = 'Season Planner';
const String kProductTagline = 'Blocks, weeks and peaks';
const String kBrandLetter = '';

/// Тема продукта: tools / seasonplan. Бренд: Rainbet.
const bool kIsLight = false;
const double kRadius = 20;

const String kDisplayFont = 'AppDisplay';
const String kBodyFont = 'AppDisplay';

const Color cBg = Color(0xFF121B27);
const Color cBgDeep = Color(0xFF0A1018);
const Color cSurface = Color(0xFF1E2C3F);
const Color cEdge = Color(0xFF505A69);
const Color cAlt = Color(0xFF38BDF8);

/// Акцент — от бренда, поэтому иконка перекликается с интерфейсом.
const Color cAccent = Color(0xFF868AFF);
const Color cAccent2 = Color(0xFFF4FDFF);